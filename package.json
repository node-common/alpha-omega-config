{
  "name": "alpha-omega-conf",
  "description": "Configurator library. Add this configurator to your project or framework and manage multiple configurations in better way",
  "repository": {},
  "license": "Apache-2.0",
  "version": "0.1.8",
  "author": "Anton Repin <ar@xdire.io>",
  "main": "./dist/main.js",
  "types": "./dist/main.d.ts",
  "dependencies": {
    "@types/config": "0.0.32",
    "@types/node": "^8.10.9",
    "config": "^1.30.0",
    "ts-exceptions": "^1.0.1",
    "typescript": "^2.8.3"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.48",
    "@types/node": "~8",
    "mocha": "^5.1.1",
    "ts-node": "^4.1.0",
    "typescript": "^2.8.3"
  },
  "scripts": {
    "prepare": "npm run build",
    "build": "tsc",
    "postinstall": "tsc",
    "test": "tsc && mocha -r ts-node/register test/**/*.ts"
  }
}
